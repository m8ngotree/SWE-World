# Crawl repositories with full commit history

# example usage for swebench-verified
python crawl_repo_modify_reponame.py \
    --json .repos_list/swebench_verified_repos.json \
    --target "YOUR_TARGET_DIR" \
    --workers 40